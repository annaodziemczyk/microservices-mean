### Installation 
``` 
cd mean
cp .env.example .env
npm install
npm start (for development)
```
### Docker based 
``` 
cd mean
cp .env.example .env
docker-compose up -d
```
### Credits 
https://github.com/linnovate/mean
